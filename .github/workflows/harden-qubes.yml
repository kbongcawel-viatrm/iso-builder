name: Harden Config

on:
  workflow_dispatch: 

permissions: write-all

jobs:
    harden-configuration:
     runs-on: [self-hosted, qubes-debian]
    
     steps:
      - name: Checkout scripts from main branch
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Run scripts/sysprep as root
        run: |
          set -euo pipefail 
          sudo chmod +x scripts/sysprep >/dev/null 2>&1
          sudo bash scripts/sysprep >/dev/null 2>&1
          echo "Packages have been upgraded" 
          
      - name: Copy and configure system-freeze.sh
        run: |
          set -euo pipefail
          sudo cp scripts/system-freeze.sh /usr/local/bin/system-freeze.sh >/dev/null 2>&1
          sudo chmod +x /usr/local/bin/system-freeze.sh >/dev/null 2>&1
          echo "Packages have been upgraded"

      - name: Create systemd service file
        run: |
          set -euo pipefail
          sudo mkdir -p /etc/systemd/system >/dev/null 2>&1
          sudo tee /etc/systemd/system/system-freeze.service > /dev/null << 'EOF'
          [Unit]
          Description=Freeze Kernel and Enforce Read-Only Boundaries
          DefaultDependencies=no
          After=local-fs.target
          Before=sysinit.target systemd-sysctl.service
          Conflicts=shutdown.target

          [Service]
          Type=oneshot
          RemainAfterExit=yes
          ExecStart=/usr/local/bin/system-freeze.sh
          StandardOutput=journal+console

          [Install]
          WantedBy=sysinit.target
          EOF
          echo "Packages have been upgraded"

      - name: Enable system-freeze service
        run: |
          set -euo pipefail
          sudo systemctl daemon-reload >/dev/null 2>&1
          sudo systemctl enable system-freeze.service >/dev/null 2>&1
          echo "Packages have been upgraded"
          
      - name: Harden Grub
        run: |
          set -euo pipefail
          sudo chmod +x scripts/secure-grub-unrestricted.sh >/dev/null 2>&1
          sudo bash scripts/secure-grub-unrestricted.sh {{ secrets.GRUB_PW }} {{ secrets.GRUB_ADMIN }} >/dev/null 2>&1
          echo "Packages have been upgraded"
      
      - name: TPM Check
        run: |
          set -euo pipefail
          sudo chmod +x scripts/tpm_enable_debian.sh >/dev/null 2>&1
          sudo bash scripts/tpm_enable_debian.sh >/dev/null 2>&1
          echo "Packages have been upgraded"
          
      - name: Remove scripts folder
        run: |
          set -euo pipefail
          sudo rm -rf scripts >/dev/null 2>&1
          echo "Packages have been removed"
